import { TxtShortPipePipe } from './txt-short-pipe.pipe';

describe('TxtShortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TxtShortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
